version: '3.8'

services:
  siglip-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TRANSFORMERS_CACHE=/app/cache
    volumes:
      - model-cache:/app/cache
    restart: unless-stopped

volumes:
  model-cache:
